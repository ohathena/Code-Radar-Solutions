#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    fgets(str, sizeof(str1), stdin);
    int n = strlen(str1);

    if (strchr(str, 'a') != NULL) {
        printf("No");
         return 0;
    } 
    else{
        printf("Yes")
    }
    return 0;
}