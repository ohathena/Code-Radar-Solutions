#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    char ch = 'o';

    if (strchr(str, a) != NULL) {
        printf("No");
         return 0;
    } 
    else{
        printf("Yes")
    }
    return 0;
}