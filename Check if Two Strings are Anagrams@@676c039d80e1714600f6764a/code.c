#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(){
    char str1[100];
    char str2[100];
    scanf("%s" , &str1);
    scanf("%s" , &str2);
    int n = strlen(str1);
    int y = strlen(str2);

    if(n!=y){
        printf("No");
        return 0;
     }
     else{

    //    for(int i = 0 ; i<n ; i++){
    //     for(int j = 0 ; j < y ; j++){
    //         if(str1[i]==str[j]){

    //         }
    //     }
    //    }
     }

   
}